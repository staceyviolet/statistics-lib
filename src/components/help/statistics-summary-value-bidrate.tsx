import * as React from 'react';

export const HelpStatisticsSummaryValueBidRate:React.FC = () => {
    return (
        <div>
          <pre style={{ color: '#ffffff' }}>Bids / Bid Requests in %</pre>
        </div>
    );

}
