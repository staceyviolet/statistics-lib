import * as React from 'react';

export const HelpStatisticsSummaryValueRevenue: React.FC = () => {
    return (
        <div>
          <pre style={{ color: '#ffffff' }}>Sum of Impression CPMs / 1000</pre>
            <div>For detailed statistics on revenue in currencies other than USD/EUR see Revenue Report.
            </div>
        </div>
    );
}
